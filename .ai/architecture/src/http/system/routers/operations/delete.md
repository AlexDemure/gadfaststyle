# HTTP System Router Operation: Delete

Используй `delete.py` для mutation-ручек удаления в `system`.

- Handler обычно называется `command`.
- Endpoint только вызывает usecase и не содержит бизнес-логики.
