# HTTP Public Router Operation: Delete

Используй `delete.py` для mutation-ручек удаления в `public`.

- Handler называется `command`.
- Статус и тело ответа повторяй по ближайшему доменному примеру.
- Endpoint только вызывает usecase и не содержит бизнес-логики.
