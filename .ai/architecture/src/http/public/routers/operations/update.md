# HTTP Public Router Operation: Update

Используй `update.py` для mutation-ручек обновления в `public`.

- Handler называется `command`.
- Схема запроса должна явно описывать изменяемые поля.
- Endpoint только вызывает usecase и не делает прикладных проверок.
