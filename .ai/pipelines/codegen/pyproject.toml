[project]
name = "ai-codegen-pipeline"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "langchain-openai",
  "langgraph",
  "langsmith"
]

[project.scripts]
ai-codegen-pipeline = "main:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
