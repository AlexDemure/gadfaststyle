## Правила

- Тесты размещай в зеркальной структуре относительно production-кода.
- Используй `pytest`.
- Используй `tests.faker.fake` и фабрики из `tests/factories/`, если нужны данные.

## Запрещено

- Не пиши тестовые данные вручную, если подходящая фабрика уже существует.
