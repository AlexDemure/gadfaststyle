## Правила

- Используй `create.py` для mutation-ручек создания в `system`.
- Handler обычно называется `command`.
- Статус, тело ответа и ошибки повторяй по принятому паттерну `system`.
- Endpoint только вызывает usecase.
