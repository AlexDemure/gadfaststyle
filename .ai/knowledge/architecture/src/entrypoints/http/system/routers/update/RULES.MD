## Правила

- Используй `update.py` для mutation-ручек обновления в `system`.
- Handler обычно называется `command`.
- Схема запроса явно описывает изменяемые поля.
- Endpoint только вызывает usecase.
