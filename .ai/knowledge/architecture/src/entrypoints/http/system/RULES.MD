## Правила

- Новый домен `system` раскладывай по пакетам `routers/<domain>/`, `deps/<domain>/`, `schemas/`.
- Домен подключай через `registry.py`.
- Если после удаления домена пакет пустеет, удаляй пустые папки и обновляй `registry.py`.
