## Правила

- В `common` храни только реально общий HTTP-код.
- Общие request/response/pagination-схемы размещай здесь, а не дублируй по контурам.
- Если код нужен только одному контуру или одному домену, не выноси его в `common`.

