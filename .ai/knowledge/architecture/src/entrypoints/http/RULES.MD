## Правила

- HTTP-код размещай по контурам `common`, `public`, `system`.
- Новый домен раскладывай по пакетам `routers`, `deps`, `schemas` внутри нужного контура.
- Для новых выборок используй паттерн `search`, а не `list`.
