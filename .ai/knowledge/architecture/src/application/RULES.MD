## Правила

- Используй инструкции этой папки как основной источник структуры application-слоя.
- В application-слое не вводи обязательный агрегирующий экспорт через `__init__.py`, если он не задан локальным пакетом явно.
