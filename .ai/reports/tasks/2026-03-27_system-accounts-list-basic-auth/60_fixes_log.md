# Fixes Log

## Устраненные замечания

- Usecase реализован через `paginated(...)` с сортировкой `created desc`.
- `basic` подключен на уровне `system` include_router для домена `accounts`.
- Тест возвращает исходные значения `settings.AUTHENTICATION_*` после выполнения.

## Неустраненные замечания

- Автоматическая проверка зависит от доступности локальной Postgres БД.
