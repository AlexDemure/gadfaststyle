# Review Log

## Замечания

1. Проверить, что сортировка и пагинация реально применяются через `repository.account.paginated(...)`, а не через неупорядоченный `all()`.
2. Проверить, что system-router защищен на уровне include_router и не требует дублирования `Depends(basic)` в каждом endpoint.
3. Проверить, что тест восстанавливает `settings.AUTHENTICATION_*` после выполнения.

## Риски

- Если локальная тестовая БД недоступна, интеграционный тест нельзя будет подтвердить автоматически в этом прогоне.
