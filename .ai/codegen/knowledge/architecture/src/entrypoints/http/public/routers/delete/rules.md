## Правила

- Используй `delete.py` для ручек удаления в `public`.
- Handler, статус и тело ответа повторяй по принятому паттерну домена.
- Endpoint только вызывает usecase и не содержит бизнес-логики.
