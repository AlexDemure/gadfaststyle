## Описание

`http/collections/enums` описывает enum для общего HTTP-слоя.

## Правила

- Enum используй для кодов, режимов и фиксированных HTTP-настроек.
- Значения enum не должны зависеть от бизнесовой сущности.
- Не добавляй сюда функции рантайма.

## Примеры

```python
import enum
```
