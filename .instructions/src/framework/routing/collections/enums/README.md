## Описание

`framework/routing/collections/enums` описывает enum routing-слоя.

## Правила

- Enum используй для фиксированных routing-режимов и параметров.
- Значения enum не должны зависеть от конкретной ручки.
- Не добавляй сюда runtime-логику.

## Примеры

```python
import enum
```
