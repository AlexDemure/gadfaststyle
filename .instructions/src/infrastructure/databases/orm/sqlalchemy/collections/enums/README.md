## Описание

`sqlalchemy/collections/enums` описывает enum ORM-слоя.

## Правила

- Enum используй для фиксированных режимов ORM-поведения.
- Значения enum не должны зависеть от конкретной таблицы.
- Не размещай здесь функции рантайма.

## Примеры

```python
import enum
```
