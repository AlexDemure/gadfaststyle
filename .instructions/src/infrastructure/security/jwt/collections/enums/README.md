## Описание

`jwt/collections/enums` описывает enum JWT-слоя.

## Правила

- Enum используй для фиксированных типов токенов и режимов.
- Значения enum не должны зависеть от предметной сущности.
- Не добавляй сюда runtime-логику.

## Примеры

```python
import enum
```
