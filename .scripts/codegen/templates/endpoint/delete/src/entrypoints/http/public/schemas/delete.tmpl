class Delete{{ entity }}(Public, Request, Command):
    # TODO: replace body with real command fields when delete is not by plain id.
    id: IntRef

    def deserialize(self, **kwargs: typing.Any) -> dict[str, typing.Any]:
        data = self.model_dump(by_alias=True)
        data.update(kwargs)
        return data
