files:
- path: src/entrypoints/http/public/routers/{{ collection }}/__init__.py
  mode: w
  content: |
    from .registry import router


    __all__ = [
        "router",
    ]
- path: src/entrypoints/http/public/routers/{{ collection }}/registry.py
  mode: w
  content: |
    from src.framework.routing import APIRouter


    router = APIRouter()
- path: src/entrypoints/http/public/routers/{{ collection }}/{{ operation }}.py
  mode: w
  content: |
    from fastapi import Body
    from fastapi import Depends
    from fastapi import status

    from src.application.usecases.{{ collection }}.exists import Usecase
    from src.entrypoints.http.public.deps.{{ collection }}.exists import dependency
    from src.entrypoints.http.public.schemas import Exists{{ model }}
    from src.framework.routing import APIRouter


    router = APIRouter()


    @router.post(
        "/{{ collection }}:exists",
        description="Exists {{ table }}",
        status_code=status.HTTP_200_OK,
        response_model=bool,
    )
    async def query(
        body: Exists{{ model }} = Body(...),
        usecase: Usecase = Depends(dependency),
    ) -> bool:
        exists = await usecase(**body.deserialize())
        return exists
- path: src/entrypoints/http/public/routers/{{ collection }}/registry.py
  mode: a
  content: |
    from . import {{ operation }}
- path: src/entrypoints/http/public/routers/{{ collection }}/registry.py
  mode: a
  content: |
    router.include_router({{ operation }}.router)
- path: src/entrypoints/http/public/routers/registry.py
  mode: a
  content: |
    from . import {{ collection }}
- path: src/entrypoints/http/public/routers/registry.py
  mode: a
  content: |
    router.include_router({{ collection }}.router, tags=["{{ model }}"])
